---
layout: "content.njk"
layoutType: page
pagination:
  data: collections.postsByYear
  size: 1000
  alias: posts
  reverse: true
---
<article class="post-full post page">

  <header class="post-full-header">
    <h1 class="post-full-title">{{ title }}</h1>
  </header>

  <section class="post-full-content">

    <div class="post-content">

      <!-- BEGIN ARCHIVE PAGE -->

      <h2>Archive</h2>

      <p>I've been blogging since before it was called blogging, but this archive only goes back to 2013. That's probably for the best.</p>

      {% for year in posts %}

        <details>
          <summary><span>{{ year }}</span></summary>

          <ul>
            {% for post in collections.postsByYear[year] | reverse %}
            <li>
              <a href="{{ post.url }}">{{ post.data.title }}</a>
              {% include "separator.njk" %}
              {{ post.data.date | dateComfortableShort }}
              {% include "separator.njk" %}
              {{  post.data.tags[1] }}
            </li>
            {% endfor %}
          </ul>
        </details>

      {% endfor %}

      <!-- END ARCHIVE PAGE -->

    </div>

  </section>

</article>
